import { Anybody, Inter } from "next/font/google";

export const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const aqua = Anybody({ subsets: ["latin"], variable: "--font-aqua" });
